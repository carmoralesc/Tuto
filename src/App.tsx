function App() {
  return (
    <main className="min-h-screen bg-slate-900 flex items-center justify-center p-6">
      <section className="rounded-2xl bg-blue-600 p-8 text-white shadow-xl">
        <h1 className="text-3xl font-bold">Tailwind está funcionando</h1>
        <p className="mt-2 text-blue-100">
          Si ves este bloque con color, espacio y tipografía, la configuración
          está bien.
        </p>
      </section>
    </main>
  );
}

export default App;
